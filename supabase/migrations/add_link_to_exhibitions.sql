-- ================================================================
-- exhibitions 테이블에 link 컬럼 추가
-- 용도: 전시회 외부 링크 (전시 사이트, 아카이브 페이지 등)
--
-- 실행: Supabase Dashboard → SQL Editor에서 이 파일 내용을 붙여넣고 실행
-- ================================================================

ALTER TABLE exhibitions
  ADD COLUMN IF NOT EXISTS link TEXT;

-- 기존 데이터에는 NULL이 들어가며, 정상 동작합니다.
-- 입력 검증은 애플리케이션 레이어(actions.ts)에서 처리합니다.
